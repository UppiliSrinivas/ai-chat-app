/**
 * Display-only mirrors of the server's caps. The server is the enforcer;
 * these let the UI swap controls before a request is refused.
 */
export const MAX_CHAT_TOKENS = 20_000
export const MAX_CHATS_PER_PROJECT = 5
